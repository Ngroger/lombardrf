🔧 1. Подготовка

Нужно:

Аккаунт GitHub (чтобы скачать сайт).

Аккаунт PS.kz (с доступом к хостингу и FTP).

Программа FileZilla (или любая FTP-прога).

Доступ к GitHub-репозиторию сайта.

🧩 2. Скачивание сайта с GitHub

Зайти на GitHub → открыть нужный репозиторий.
Пример: https://github.com/username/lombard-rf

Нажать зелёную кнопку Code → Download ZIP.

Распаковать архив у себя на компьютере.

Должны появиться файлы вроде index.php, wp-content, public_html, .env и т.д.

🗂 3. Подключение к серверу PS.kz

Открыть FileZilla.

Ввести:

Host: IP или домен из PS.kz (пример: ftp.lombardrf.kz)

User: логин FTP (указан в панели PS.kz)

Password: пароль FTP

Port: 21

Нажать Quickconnect.

📤 4. Загрузка сайта на PS.kz

В правом окне (сервер) открыть папку public_html.

В левом окне (компьютер) открыть папку, куда вы распаковали сайт.

Перетащить все файлы с левой стороны (ваш сайт) → направо (сервер).

Ждать, пока всё загрузится (индикатор внизу покажет процесс).

⚙️ 5. Настройка базы данных

В PS.kz панели создать новую базу данных MySQL.

Запомнить:

Имя БД

Пользователь БД

Пароль БД

Хост (обычно localhost)

Открыть на сервере файл wp-config.php (или .env — если Laravel/Node).

Вписать туда новые данные:

define('DB_NAME', 'имябд');
define('DB_USER', 'пользователь');
define('DB_PASSWORD', 'пароль');
define('DB_HOST', 'localhost');

🧱 6. Импорт базы данных

На старом хостинге (Beget) зайти в phpMyAdmin → выбрать нужную БД → нажать Экспорт → сохранить .sql файл.

На новом (PS.kz) в phpMyAdmin → выбрать новую БД → Импорт → загрузить тот .sql файл.

🌐 7. Проверка домена

В панели PS.kz указать корневую папку сайта — public_html.

Привязать домен lombardrf.kz (если не привязан).

Убедиться, что DNS обновлены (может занять до 24 часов).

Открыть сайт → проверить, всё ли грузится.

🔒 8. SSL и HTTPS

В панели PS.kz активировать SSL (Let’s Encrypt).

После активации — сайт должен открываться по https://lombardrf.kz.
